name = "inkitin-printing-hub"
compatibility_date = "2026-09-04"
pages_build_output_dir = "."
[vars]
APP_ENV = "test"
WHATSAPP_MODE = "test"
# DATABASE_URL and provider secrets must be configured as encrypted Cloudflare variables.
# R2 artwork storage will be enabled later after the storage/billing requirement is configured.
